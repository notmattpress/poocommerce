/**
 * External dependencies
 */
import { WebPreview } from '@poocommerce/components';

export const Basic = () => (
	<WebPreview
		src="https://themes.poocommerce.com/?name=galleria"
		title="My Web Preview"
	/>
);

export default {
	title: 'PooCommerce Admin/components/WebPreview',
	component: WebPreview,
};
